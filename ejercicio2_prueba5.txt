#!/usr/bin/env python
# -*- coding: utf-8 -*-

import json
from pprint import pprint

#1. Lista las distintas razas que hay.


fichero_perros = open('/home/usuario/Documentos/perros.json','r')
perro = json.load(fichero_perros)
raza_def = ()

for x in perro["animales"]["animal"]:
	raza = x["descripcion"]
	for tipos in raza_def:
		while tipos != raza: 
			raza_def.append(raza)

			print raza_def


#2. ¿Cuantos perros grandes hay?

dicc_tam=[]
for x in perro["animales"]["animal"]:
	tam = (x["tamanyo"])
	dicc_tam.append(tam)
	contador_grande = 0
	for x in dicc_tam:
		if x == "Grande":
			contador_grande = contador_grande + 1

print "2. ¿Cuantos perros grandes hay?"
print "Hay un total de",contador_grande,"perros grandes"
	
	
#3. Mostrar raza y tamaño de la cadena siguiente:


cadena = raw_input("Introduce una cadena para buscar: ")

for x in perro["animales"]["animal"]:
	raza = x["descripcion"].lower()
	tam = x["tamanyo"].lower()
	if raza == cadena or tam == cadena:
		print "raza:",x["descripcion"],"tamaño:",x["tamanyo"]

	
			

